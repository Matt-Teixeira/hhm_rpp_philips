# Running

Superseded 2026-08-26 by the fleet paradigm — see `CLAUDE.md` (this repo) for
current run/build/release commands, and `data_acquisition/docs/migration_CLAUDE.md`
Part 1 for the conventions.

The instructions that used to live here described the pre-migration setup
(root-running `app` service, `npm run` via `bash -lc`, `npm ci` into the
shared `/opt/resources/node_mod_cache` — all retired) and are kept only in git
history.
